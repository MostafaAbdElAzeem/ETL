name: etl
version: '1.0'
config-version: 2

profile: dummy_profile  # this matches the profile in ~/.dbt/profiles.yml

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"